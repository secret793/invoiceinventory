Options -Indexes
RewriteEngine On

# Route all requests to public/index.php (when hosted from /backend)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/index.php [L,QSA]
