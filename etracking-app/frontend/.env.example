# Copy this file to .env.production before running: npm run build
#
# Set this to the root URL of your cPanel backend subdomain/addon domain.
# Do NOT include /api at the end — the app adds that automatically.
#
# Example: your backend lives at https://api.yourdomain.com
VITE_API_URL=https://api.yourdomain.com
